import 'package:flutter/material.dart';

class ColorManager {
  static const primary = Color(0xFF21356A);
  static const secondary = Color(0xFF0000);
  static const white = Colors.white;
  static const red = Colors.red;
  static const green = Colors.green;
}
